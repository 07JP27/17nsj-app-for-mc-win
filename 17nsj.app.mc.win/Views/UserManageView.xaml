<Window x:Class="_17nsj.app.mc.win.Views.UserManageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_17nsj.app.mc.win.Views"
        mc:Ignorable="d"
        Title="UserManageView" Height="300" Width="400" Loaded="ViewLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30px" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
            <Button Grid.Column="3" Content="編集" Click="UserEditButtonClicked"/>
        </Grid>
        <DataGrid Grid.Row="1" Margin="5" ItemsSource="{Binding Path=FilteredUserList, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding Path=SelectedUser, UpdateSourceTrigger=PropertyChanged}" MouseDoubleClick="UserDoubleClicked">
            
        </DataGrid>
    </Grid>
</Window>
